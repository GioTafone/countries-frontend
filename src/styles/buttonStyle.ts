import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  root: {
    fontSize: 12,
    color: '#343634',
    '&:hover': {
      backgroundColor: '#f2eeed',
    },
  },
})

export { useStyles }
