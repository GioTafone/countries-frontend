import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  root: {
    fontSize: 12,
    '&:hover': {
      backgroundColor: '#f2eeed',
    },
  },
})

export { useStyles }
